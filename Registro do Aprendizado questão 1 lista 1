Essa questão foi feita completamente de forma manual, basicamente utilizei a W3school para busca de informações de sintaxe, extras como a biblioteca <stdlib.h> foram de conversas com colegas

#include <stdio.h>

int main(){
    // Declaração das variáveis para armazenar as duas datas
    int dia1, mes1, ano1;
    int dia2, mes2, ano2;
    
    // Lê as duas datas no formato DD/MM/AAAA. 
    // As barras (/) no scanf obrigam o usuário a digitar exatamente nesse formato.
    scanf("%d/%d/%d %d/%d/%d", &dia1, &mes1, &ano1, &dia2, &mes2, &ano2);
    
    // ==========================================
    // CÁLCULO DO AMOR
    // ==========================================
    int valor_amor;
    // Soma todos os componentes das duas datas
    valor_amor = dia1 + mes1 + ano1 + dia2 + mes2 + ano2;

    // Multiplica por 7 e pega o resto da divisão por 101 (% 101).
    // Isso garante que o resultado seja sempre um número entre 0 e 100.
    valor_amor = (valor_amor * 7) % 101;
    
    printf("Amor: %d%% ", valor_amor);
    
    // Classifica o resultado do amor e imprime a mensagem correspondente
    if(valor_amor < 20){
         printf("Pessimo dia para se apaixonar.");
    } else if(valor_amor <= 40 && valor_amor >= 20){
        printf("Melhor manter o coracao <3 longe de perigo.");
    } else if(valor_amor < 70 && valor_amor > 40){
        printf("Se o papo e as ideias baterem, esta liberado pensar em algo.");
    } else if(valor_amor <= 80 && valor_amor >= 70){
         printf("Saia com o coracao aberto, mas lembre, nem toda troca de olhar em onibus e sinal de romance.");
    } else {
         printf("Um dia deslumbrantemente lindo para amar. Ps: Cuidado com a intensidade.");
    }
    printf("\n");
    
    // ==========================================
    // CÁLCULO DA SORTE
    // ==========================================
    int sorte;
    // Fórmula customizada para a sorte.
